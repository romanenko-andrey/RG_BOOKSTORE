.col-xs-6.col-sm-3
  .general-thumb-wrap
    .thumbnail.general-thumbnail
      = image_tag title_photo(book), alt: book.alt, class: ['img-shadow', 'general-thumbnail-img']
      .thumb-hover
        .general-thumb-link-wrap
          = link_to book_path(id: book.id), class: "thumb-hover-link" do
            %i.fa.fa-eye.thumb-icon{'aria-hidden': "true"}
          %a.thumb-hover-link{href: "#"}
            %i.fa.fa-shopping-cart.thumb-icon
  .general-thumb-info
    %p.title= book.name
    %p.font-16.in-gold-500= to_euro(book.price)
    %span.divider-sm
    %p.lead.small= authors_to_string(book.authors)